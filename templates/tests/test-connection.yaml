apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-tutorial.fullname" . }}-test-connection"
  labels:
{{ include "helm-tutorial.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-tutorial.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
